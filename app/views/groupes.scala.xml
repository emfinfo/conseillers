@(groupes: List[models.Groupe])
<groupes>
  @for(groupe <- groupes) {
  <groupe>
    <id>@groupe.getPk()</id>
    <abrev>@groupe.getAbrev()</abrev>
    <nom>@groupe.getNom()</nom>
  </groupe>
  }
</groupes>
